---
import { MotionDiv } from "./ui/motion";

const variants = {
    hidden: { opacity: 0, y: 20 },
    visible: { opacity: 1, y: 0 },
};
---

<section class="py-20 px-4 lg:px-8" aria-labelledby="about-heading">
    <MotionDiv
        client:visible
        className="max-w-3xl mx-auto text-center"
        initial="hidden"
        whileInView="visible"
        viewport={{ once: true, amount: 0.5 }}
        variants={{
            hidden: { opacity: 0, y: 50 },
            visible: { opacity: 1, y: 0 }
        }}
        transition={{ duration: 0.8, ease: "easeOut" }}
    >
        <h2 id="about-heading" class="text-4xl md:text-5xl font-bold text-white mb-8">About Vertix</h2>
        <p class="text-xl text-white/80 leading-relaxed">
            Vertix is a revolutionary platform that empowers individuals to truly own, trade, and monetize their digital identity. 
            We're building the future where gamers, creators, and digital entrepreneurs have complete control over their digital assets.
        </p>
    </MotionDiv>
</section>